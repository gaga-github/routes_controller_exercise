<h2> I AMA View </h2>
<h2> Hello World </h2>
<p>
  <% first_product = Product.first %>
  Product Name: "<%= first_product.name %>" costs $<%= first_product.price %>
</p>
    <p>
      The seller for this product is named '<%= first_product.user.name %>'
    </p>
<p>
	Total Products on this site <b><%= Product.count %></b>
</p>
<p>
	<% total_products = Product.includes(:user).all %>
</p>
<ul>
 <% total_products.each do |product| %>
	<li>
	 Product Name: <%= product.name %> Costs <%= product.price %> Sold by: <%= product.user.name %>
	</li>
 <% end %>
</ul>
